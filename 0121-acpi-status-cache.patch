From 2394318d6425758ebb628d2b41cf8fe518312e54 Mon Sep 17 00:00:00 2001
From: Arjan van de Ven <arjan@linux.intel.com>
Date: Sun, 14 May 2017 23:53:08 +0000
Subject: [PATCH 121/129] acpi: status cache

---
 drivers/acpi/scan.c     | 3 ++-
 include/acpi/acpi_bus.h | 2 ++
 2 files changed, 4 insertions(+), 1 deletion(-)

diff --git a/drivers/acpi/scan.c b/drivers/acpi/scan.c
index bd1c59fb0e17..014614d3b9cc 100644
--- a/drivers/acpi/scan.c
+++ b/drivers/acpi/scan.c
@@ -1955,7 +1955,8 @@ static void acpi_bus_attach(struct acpi_device *device)
 	if (ACPI_SUCCESS(acpi_bus_get_ejd(device->handle, &ejd)))
 		register_dock_dependent_device(device, ejd);
 
-	acpi_bus_get_status(device);
+	if (!device->status_valid)
+		acpi_bus_get_status(device);
 	/* Skip devices that are not present. */
 	if (!acpi_device_is_present(device)) {
 		device->flags.initialized = false;
diff --git a/include/acpi/acpi_bus.h b/include/acpi/acpi_bus.h
index 0300374101cd..63bee1e0a86a 100644
--- a/include/acpi/acpi_bus.h
+++ b/include/acpi/acpi_bus.h
@@ -373,6 +373,7 @@ struct acpi_device {
 	struct list_head wakeup_list;
 	struct list_head del_list;
 	struct acpi_device_status status;
+	int status_valid;
 	struct acpi_device_flags flags;
 	struct acpi_device_pnp pnp;
 	struct acpi_device_power power;
@@ -466,6 +467,7 @@ static inline void *acpi_driver_data(struct acpi_device *d)
 static inline void acpi_set_device_status(struct acpi_device *adev, u32 sta)
 {
 	*((u32 *)&adev->status) = sta;
+	adev->status_valid = 1;
 }
 
 static inline void acpi_set_hp_context(struct acpi_device *adev,
-- 
2.20.1

