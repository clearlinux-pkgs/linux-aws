--- linux-4.11.1/drivers/acpi/bus.c~	2017-05-14 12:06:16.000000000 +0000
+++ linux-4.11.1/drivers/acpi/bus.c	2017-05-15 19:54:28.636058820 +0000
@@ -119,6 +119,7 @@
 		return -ENODEV;
 
 	acpi_set_device_status(device, sta);
+	printk("_STA called, ADR is %p\n", device->pnp.bus_address);
 
 	if (device->status.functional && !device->status.present) {
 		ACPI_DEBUG_PRINT((ACPI_DB_INFO, "Device [%s] status [%08x]: "
--- linux-4.11.1/drivers/acpi/bus.c~	2017-05-15 19:54:28.000000000 +0000
+++ linux-4.11.1/drivers/acpi/bus.c	2017-05-15 20:10:57.535240186 +0000
@@ -98,6 +98,7 @@
 	acpi_status status;
 
 	status = acpi_evaluate_integer(handle, "_STA", NULL, sta);
+	printk("_STA called, ADR is %p\n", device->address);
 	if (ACPI_SUCCESS(status))
 		return AE_OK;
 
